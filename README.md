配置：vscode + claude code + 百炼GLM5

指令：

> 1. 在当前目录中，使用 Python 3.14 在符合 Python 工程最佳实践的前提下，实现 24 点出题器功能，仅需在 CLI 下出题并提供显示答案的功能。需添加 .gitignore 文件，避免不要的文件或信息泄露至 github
> 2. 基于当前项目代码，指出十个可以改进的地方，在根目录生成 md 文档保存这些建议。
> 3. 当前计算逻辑中，会出现等价计算方式分别算为一次答案，例如：(11-5)*2*2 与 2*2*(11-5) 均是 11、5、2、2 的答案，调整代码逻辑，将这样等价计算方式仅取一种。
> 4. 依然存在等价情况，例如：4 10 2 5 的 ((2*5)+10)+4 与 ((2*5)+4)+10。此外，答案中减少不必要的括号

以下均为 AI 生成。
> # 24点游戏出题器
> 
> 一个命令行工具，用于生成和求解24点数学游戏题目。
> 
> ## 简介
> 
> 24点游戏是一种经典的数学益智游戏：给定4个数字（通常为1-13），使用加、减、乘、除四则运算，使每个数字必须且> 只能使用一次，最终结果等于24。
> 
> ## 功能特性
> 
> - 交互式命令行界面
> - 自动生成随机题目（保证有解）
> - 显示所有可能的解法
> - 支持分数运算（如 `8/(3-8/3)=24`）
> 
> ## 安装
>
> 要求 Python 3.14 或更高版本，使用 uv 进行包管理。
>
> ```bash
> # 克隆仓库
> git clone <repository-url>
> cd PY-24-Point-Generator
>
> # 安装依赖（包括开发依赖）
> uv sync --all-extras
> ```
> 
> ## 使用方法
>
> ### 运行程序
>
> ```bash
> uv run python -m twenty_four
> ```
>
> 或者直接使用命令：
>
> ```bash
> uv run twenty-four
> ```
> 
> ### 交互界面
> 
> ```
> =====================================
>         24点游戏出题器 v1.0.0
> =====================================
> 
> 规则说明：
>   给定4个数字（1-13），使用 +、-、*、/ 四则运算
>   每个数字必须且只能使用一次，使结果等于24
> 
> -------------------------------------
>               主菜单
> -------------------------------------
>   [1] 生成新题目
>   [2] 显示答案
>   [q] 退出程序
> -------------------------------------
> 当前题目：8 3 8 3
> 状态：（有 1 种解法）
> 
> 请输入选项:
> ```
> 
> ## 开发
>
> ### 运行测试
>
> ```bash
> uv run pytest
> ```
>
> ### 类型检查
>
> ```bash
> uv run mypy src/twenty_four
> ```
>
> ### 代码格式化
>
> ```bash
> uv run ruff format src tests
> ```
> 
> ## 许可证
> 
> MIT License (AI写的，不作数)